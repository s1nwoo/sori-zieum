spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sorizieum
    username: sorizieum
    password: sorizieum

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

server:
  port: ${PORT:8080}